package ${object.packageStr}.service;
import ${object.packageStr}.dao.${object.name}Dao;
import ${object.packageStr}.model.db.${object.name}Model;
import org.springframework.stereotype.Service;
import com.yipeng.framework.core.service.BaseService;

/**
* @author ${base.authorName}
* email:${base.authorEmail}
* AUTO-GENERATE BY PROJECT-BUILDER
**/
@Service
public class ${object.name}Service extends BaseService<${object.name}Model, ${object.name}Dao> {
}